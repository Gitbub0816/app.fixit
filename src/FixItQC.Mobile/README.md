# FixItQC.Mobile (.NET MAUI Hybrid scaffold)

Targets:
- Windows
- iOS
- Android

Shared web UI surfaces are hosted in a MAUI BlazorWebView shell.
Camera/media capture services are abstracted so production-native capture and background audio can be swapped per platform.
